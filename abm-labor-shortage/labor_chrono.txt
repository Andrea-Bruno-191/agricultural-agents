##DOCUMENTATION
# file format:
# - first column is the week number since simulation start
# - second columne is a comma-separated event description
# if the second column keyword is SET then we modify the
# utils.global_state dictionary in that way.  For example:
# SET,n_jobs_total,300 means that in file labor_utils.py
# the dictionary value global_state['n_jobs_total'] will
# be set to 300.  This in turn will affect employment simulation
# and immigration.
##START_OF_EVENTS
# initial values
0    SET,farmer_wage_weekly,750
0    SET,immigration_prob_percent,5
0    SET,n_jobs_total,300
# 0    PERIODIC,10,ICE_RAID,10
# 0    PERIODIC,4,RENT_DUE
# 0    PERIODIC,52,TAX_DUE
# 0    PERIODIC_INCREMENT,52,n_jobs_total,10
# ICE raid, 4 workers removed
2    ICE_RAID,4
12   INCREMENT,n_jobs_total,10
24   INCREMENT,n_jobs_total,3
# good demand, more employment
36   INCREMENT,n_jobs_total,5
48   INCREMENT,n_jobs_total,5
# a crash which decreases employment
70   ICE_RAID,3
100  ICE_RAID,4
200  ICE_RAID,2
210  INCREMENT,n_jobs_total,10
310  INCREMENT,n_jobs_total,-100
322  INCREMENT,n_jobs_total,30
334  INCREMENT,n_jobs_total,3
346  INCREMENT,n_jobs_total,4
410  INCREMENT,n_jobs_total,80
# new president, fewer ICE raids
# 500  PERIODIC,10,ICE_RAID,1
